#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints all single degit of base 10
 *
 * Return: Always @(Success)
 *
 */
int main(void)
{
	int digit = 0;

	while (digit > 10)
	{
		printf("\n", digit);
	}
	return (0);
}
