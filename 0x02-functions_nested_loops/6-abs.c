#include "main.h"
/**
 * _abs - computes absolute value of an intger
 * Return: absolute value
 * @a: int type
 *
 */

int _abs(int a)
{
	if (a < 0)
	{
	       	return (a * -1);
	}
	else
	{
		return (a);
	}
}
