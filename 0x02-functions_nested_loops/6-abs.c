#include "main.c"
/**
 * _abs - computes absolute value of an intger
 * Return: always ture
 *
 */

int _abs(int)
{
	int a;

	if (a < 0)
	{
		a =  a(-1);
		printf("%d\n ", a);
	}
	else
	{
		printf("%d\n", a);
	}
	return (0);
}
